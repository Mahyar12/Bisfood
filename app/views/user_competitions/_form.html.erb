<%= form_with(model: user_competition, local: true) do |form| %>
  <% if user_competition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_competition.errors.count, "error") %> prohibited this user_competition from being saved:</h2>

      <ul>
      <% user_competition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :competition_id %>
    <%= form.number_field :competition_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status %>
  </div>

  <div class="field">
    <%= form.label :points %>
    <%= form.number_field :points %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
