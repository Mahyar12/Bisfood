<%= form_with(model: category_answer, local: true) do |form| %>
  <% if category_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_answer.errors.count, "error") %> prohibited this category_answer from being saved:</h2>

      <ul>
      <% category_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id, id: :category_answer_category_id %>
  </div>

  <div class="field">
    <%= form.label :answer_id %>
    <%= form.number_field :answer_id, id: :category_answer_answer_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
