<%= form_with(model: answer, local: true) do |form| %>
  <% if answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(answer.errors.count, "error") %> prohibited this answer from being saved:</h2>

      <ul>
      <% answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :answer_question_id %>
    <%= form.number_field :answer_question_id, id: :answer_answer_question_id %>
  </div>

  <div class="field">
    <%= form.label :category_answer_id %>
    <%= form.number_field :category_answer_id, id: :answer_category_answer_id %>
  </div>

  <div class="field">
    <%= form.label :answer_type %>
    <%= form.text_field :answer_type, id: :answer_answer_type %>
  </div>

  <div class="field">
    <%= form.label :answer_type_id %>
    <%= form.number_field :answer_type_id, id: :answer_answer_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
