<%= form_with(model: question, local: true) do |form| %>
  <% if question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul>
      <% question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :question_content %>
  </div>

  <div class="field">
    <%= form.label :qtype_id %>
    <%= form.number_field :qtype_id, id: :question_qtype_id %>
  </div>

  <div class="field">
    <%= form.label :answer_question_id %>
    <%= form.number_field :answer_question_id, id: :question_answer_question_id %>
  </div>

  <div class="field">
    <%= form.label :category_question_id %>
    <%= form.number_field :category_question_id, id: :question_category_question_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
