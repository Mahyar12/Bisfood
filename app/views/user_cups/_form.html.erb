<%= form_with(model: user_cup, local: true) do |form| %>
  <% if user_cup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_cup.errors.count, "error") %> prohibited this user_cup from being saved:</h2>

      <ul>
      <% user_cup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :cup_id %>
    <%= form.number_field :cup_id %>
  </div>

  <div class="field">
    <%= form.label :cup_count %>
    <%= form.number_field :cup_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
